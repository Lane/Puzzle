function piece_container_tests() {

	module('PieceContainer.js');
	
	// set boundary
	test('flow', function() {
		// create piece container
		var pc = _containers[0];
		var p = _pieces[0];
		var pt = _points[0];
		var pt2 = _points[1];
		// add a piece to it
		
		// rotate it
	
	});
	
}